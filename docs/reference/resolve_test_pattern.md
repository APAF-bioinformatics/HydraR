# Resolve Test Pattern (for Conditional Edges)

Resolve Test Pattern (for Conditional Edges)

## Usage

``` r
resolve_test_pattern(v, base_dir = ".")
```

## Arguments

- v:

  String or function.

- base_dir:

  String. Base directory for resolving relative paths.

## Value

A function(out) -\> Logical.
