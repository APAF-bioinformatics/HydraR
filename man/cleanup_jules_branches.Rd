% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/git_cleanup.R
\name{cleanup_jules_branches}
\alias{cleanup_jules_branches}
\title{Cleanup Stale GitHub Branches}
\usage{
cleanup_jules_branches(
  repo_root = getwd(),
  threshold_hours = 24,
  dry_run = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{repo_root}{String. Path to the git repository. Defaults to `getwd()`.}

\item{threshold_hours}{Numeric. Inactivity threshold for Jules's branches (hours). Defaults to 24.}

\item{dry_run}{Logical. If TRUE (default), identifies candidates but does not perform deletion.}

\item{verbose}{Logical. If TRUE, prints detailed status messages.}
}
\value{
A character vector of branches identified for deletion.
}
\description{
Identifies and optionally deletes stale remote branches on GitHub.
Specifically targets branches authored by 'google-labs-jules[bot]'
that have been inactive beyond a specified threshold, or any branch
already merged into the main branch.
}
\examples{
\dontrun{
# 1. Identify stale branches without deleting (Dry Run)
stale_branches <- cleanup_jules_branches(dry_run = TRUE)

# 2. Perform aggressive cleanup of stale bot branches (> 48 hours)
cleanup_jules_branches(
  threshold_hours = 48,
  dry_run = FALSE,
  verbose = TRUE
)
}
}
